name: app
description: تطبيق للتحقق من الصلاة بالتعرف على الوجه
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  prayer_times: ^2.0.0
  flutter_local_notifications: ^17.0.0
  location: ^5.0.0
  permission_handler: ^11.0.0
  google_ml_kit: ^0.16.0
  image_picker: ^1.0.0
  camera: ^0.10.5
  shared_preferences: ^2.2.0
  path_provider: ^2.1.0
  flutter_svg: ^2.0.9
  intl: ^0.18.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
